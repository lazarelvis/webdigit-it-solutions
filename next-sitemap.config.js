/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://webdigit.ro", // ← replace with your real domain
  generateRobotsTxt: true, // ← this enables robots.txt generation
  sitemapSize: 5000, // optional
};
